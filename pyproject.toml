[project]
name = "lowest-common-ancestor"
version = "0.1.0"
description = "Preprocesses a tree to answer lowest common ancestor queries in constant time."
requires-python = ">=3.10"
dependencies = []
readme = "README.md"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
